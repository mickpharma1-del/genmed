{
  "name": "supergenkart-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "echo 'Lint placeholder: configure eslint in CI'",
    "test": "node ./scripts/validate-rupee-format.mjs"
  }
}
